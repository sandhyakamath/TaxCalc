NUMERIC result;
NUMERIC accumulated;
NUMERIC deducted;
NUMERIC net;
accumulated = basic + allowance + da + hra;
deducted = deduction + cess + surcharge;
net = accumulated - deducted;
result = net * 0.2;